{
  "name": "taskflow",
  "version": "1.0.0",
  "private": true,
  "description": "Taskflow - full-stack project delivery and task operations suite",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "setup": "npm run install:all && npm run db:start && npm run db:wait && npm run prisma:generate && npm run prisma:migrate && npm run prisma:seed",
    "install:all": "npm install --include=dev && npm install --include=dev --prefix frontend",
    "build": "npm install --include=dev --prefix frontend && npm run build --prefix frontend && npx prisma generate --schema=backend/prisma/schema.prisma",
    "start": "node scripts/start-production.mjs",
    "seed": "npx prisma db seed --schema=backend/prisma/schema.prisma",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend -- --host 0.0.0.0",
    "dev:full": "concurrently -k -n BACKEND,FRONTEND -c green,cyan \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build:frontend": "npm run build --prefix frontend",
    "prisma:generate": "npx prisma generate --schema=backend/prisma/schema.prisma",
    "prisma:migrate": "npx prisma migrate deploy --schema=backend/prisma/schema.prisma",
    "prisma:seed": "npx prisma db seed --schema=backend/prisma/schema.prisma",
    "predev:full": "npm run db:start && npm run db:wait && npm run prisma:generate && npm run prisma:migrate",
    "db:start": "node scripts/start-db.cjs",
    "db:wait": "node scripts/wait-for-db.cjs"
  },
  "prisma": {
    "seed": "node backend/prisma/seed.js"
  },
  "dependencies": {
    "@prisma/client": "^6.1.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "express-rate-limit": "^8.5.1",
    "helmet": "^8.1.0",
    "hpp": "^0.2.3",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "prisma": "^6.1.0"
  }
}
